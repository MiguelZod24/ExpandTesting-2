Diagnóstico: El error ocurre porque el selector utilizado para los campos de usuario y contraseña, 'input[name="username"]' y 'input[name="password"]', es demasiado genérico y no sigue la recomendación de usar selectores robustos como getByRole() o basados en texto visible que son más confiables. Se han actualizado los selectores en el archivo para que sean más robustos y confiables. También se ajustó la navegación para incluir directamente la BASE_URL al invocar el método `page.goto()`. Adicionalmente, en las validaciones `campo_vacio()` y `valor_ingresado()`, se agregó un `wait_for_selector` para asegurarse de la existencia del elemento antes de interactuar con él.
Archivo: pages/login_page.py
