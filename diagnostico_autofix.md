Diagnóstico: El error ocurre porque el test realiza un assertion para verificar un mensaje de error, pero el login no falla. En su lugar, se realiza exitosamente y devuelve el mensaje 'You logged into a secure area!'. La causa principal del problema es que el test esperaba un fallo, pero las credenciales proporcionadas ('practice' y 'SuperSecretPassword!') son válidas. Para resolver el problema, debes ingresar credenciales incorrectas (p. ej., 'usuario_incorrecto' o 'contraseña_incorrecta') si deseas probar un caso de error, o modificar el assertion para coincidir con el mensaje esperado en caso de un login exitoso, en caso de que realmente estés probando un caso de éxito. Por último, se mejoró la función 'campo_vacio' para simplificar su lógica y hacerla más robusta.
Archivo: pages/login_page.py
