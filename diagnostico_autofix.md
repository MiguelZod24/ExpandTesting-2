Diagnóstico: El error ocurre porque el test busca un mensaje de error específico ('Este mensaje nunca aparecerá') que no está presente en la página después de un login exitoso. Además, en el archivo `pages/login_page.py` se utilizan selectores basados en atributos genéricos como `input[name='username']`. Esto se considera subóptimo en términos de mantenibilidad y robustez, ya que los selectores basados en roles o etiquetas de accesibilidad (usar `getByRole`, `aria-label`, etc.) son más estables y amigables con prácticas recomendadas de accesibilidad. Por lo tanto, se ajustaron los selectores para cumplir con las mejores prácticas de accesibilidad y robustez según las reglas establecidas.
Archivo: pages/login_page.py
