Diagnóstico: El error surge debido a que el mensaje esperado ('Este mensaje nunca aparecerá') no coincide con el texto real que aparece tras iniciar sesión correctamente ('You logged into a secure area!'). Esto ocurre porque la lógica y las credenciales utilizadas en el test ('practice', 'SuperSecretPassword!') son válidas y, por ende, la autenticación es exitosa, mostrando un mensaje inesperado. En lugar de esperar un mensaje de error inexistente tras una autenticación exitosa, se necesita asegurar que el test tiene el propósito específico requerido (fallar o tener un error real). Además, los selectores de los inputs y botones fueron ajustados para seguir las mejores prácticas de accesibilidad con `get_by_role`, alineado con las reglas establecidas.
Archivo: pages/login_page.py
