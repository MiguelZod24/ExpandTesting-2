Diagnóstico: El error se produce porque el test intenta validar un mensaje 'Este mensaje nunca aparecerá' en lugar del mensaje real que aparece tras un login exitoso ('You logged into a secure area!'). Para corregirlo, revisa la lógica del test para que se valide el mensaje correcto en función de los requerimientos. Además, se ha ajustado el uso de los métodos de Playwright a `locator` para alinearse con las mejores prácticas de estabilidad y manejo de selectores definidos como reglas globales (TestDino Golden Rule #1). También se actualizó la función 'navegar()' para incluir la BASE_URL correctamente en la construcción de la URL.
Archivo: pages/login_page.py
