Diagnóstico: El error informado se debe a que el mensaje esperado ('Este mensaje nunca aparecerá') no está presente en 'You logged into a secure area!'. Esto sucede porque la prueba está diseñada para fallar de manera intencional, lo que es correcto en este caso. Sin embargo, el código de la página no cumple con las reglas definidas: los selectores no usan get_by_role consistentemente y se accedió a atributos y valores de campos de entrada usando métodos que no son propios del contexto de Playwright moderno. Además, la base URL no se utiliza desde la configuración global. Los problemas fueron corregidos siguiendo las reglas proporcionadas, como usar get_by_role para selectores y mejorar las interacciones con métodos nativos de Playwright.
Archivo: pages/login_page.py
