Diagnóstico: El error ocurre porque la assertion está diseñada para fallar intencionalmente, tal como indica el comentario en el código: 'Esta assertion fallará intencionalmente para probar la captura de artefactos'. El mensaje buscado 'Este mensaje nunca aparecerá' no se encuentra en el texto real 'Your username is invalid!'. Aunque el fallo es esperado, se hicieron correcciones en la clase LoginPage para aplicar buenas prácticas y garantizar que todos los métodos usen locators robustos y consistentes (mediante métodos de selección de locators estándar en Playwright como `locator` en vez de métodos directos). Además, la navegación hacia la URL del login ahora usa correctamente la constante BASE_URL para evitar posibles errores al cambiar la URL base.
Archivo: pages/login_page.py
