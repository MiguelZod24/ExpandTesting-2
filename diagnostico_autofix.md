Diagnóstico: El error ocurre debido a un error de aserción intencional en el test que compara un mensaje inexistente ('Este mensaje nunca aparecerá') con el mensaje mostrado por la página después de un login exitoso ('You logged into a secure area!'). No hay error en el código, ya que el fallo fue diseñado para probar la funcionalidad de captura de artefactos. Sin embargo, los selectores para los elementos del formulario pueden mejorarse utilizando get_by_role de Playwright, lo que es más robusto y permite identificar elementos de forma más precisa y semántica, como se aplicó en la corrección del código.
Archivo: pages/login_page.py
