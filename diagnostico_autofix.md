Diagnóstico: El error del test se debe a que la validación deliberadamente esperaba un mensaje ('Este mensaje nunca aparecerá') que no coincide con el resultado real ('You logged into a secure area!'). Además, los selectores no seguían completamente la regla de usar siempre `get_by_role` en lugar de selectores de tipo CSS o XPath, lo que podría causar problemas en la robustez de las pruebas debido a cambios en el DOM. El código se ha corregido para adherirse plenamente a las recomendaciones de mejores prácticas, incluyendo el uso exclusivo de selectores accesibles y métodos como `get_by_role` en Playwright.
Archivo: pages/login_page.py
